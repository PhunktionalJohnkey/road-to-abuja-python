services:
  phunkytech-nginx-svc:
    image: nginx
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    volumes:
      - phunkytechdata:/app/data

volumes:
  phunkytechdata:
